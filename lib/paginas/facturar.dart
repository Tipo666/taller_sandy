import 'package:flutter/material.dart';

class Facturar extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Facturar"),
          centerTitle: true,
        ),
        body: Center(
          child: Text("La seccion de facturar esta en construccion."),
        ));
  }
}
